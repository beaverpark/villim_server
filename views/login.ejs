<!DOCTYPE html>
<html>
  <!-- head -->
  <%- include('header') -%>

  <body>
    <!-- navbar -->


    <div class="container form_container">
        <h4 class="text_thick"> 로그인 </h4>

        <!-- show any messages that come back with authentication -->
        <% if (message.length > 0) { %>
            <div><p class="err_msg"><%= message %></p></div>
        <% } %>

        <!-- login form -->
        <form action="/login" method="post" class="form_vertical_margin">
            <div class="row row_form">
                <div class="input-field col s12"> 
                    <i class="material-icons prefix">email</i>
                    <input id="email" name="email" type="email" class="validate">
                    <label for="email" data-error="이메일 주소가 올바르지 않습니다">이메일</label>
                </div>
                <div class="input-field col s12">
                    <i class="material-icons prefix">lock</i>
                    <input id="password" name="password" type="password" class="validate">
                    <label for="password">비밀번호</label>
                </div>
                <div class="input-field col s12">
                    <input id="remember" name="remember" type="checkbox">
                    <label for="remember">로그인 상태 유지 </label>
                </div>
            </div>
            <div class="center-align">
                <button type="submit" class="btn waves-effect waves-light card_btn red accent-2">로그인 </button>
            </div>
        </form>

        <p>아직 회원이 아니신가요? <a href="/signup">회원가입 </a></p>
        <p><a href="/">홈으로 돌아가기</a></p>
    </div>

    <!-- footer -->
    <%- include('footer') -%>
  </body>
</html>

           <!--  <form action="/" method="post">
              <div class="row">
                
                <div class="input-field col s6">
                  <i class="material-icons prefix">account_circle</i>
                  <input id="lastname" name="lastname" type="text" class="validate">
                  <label for="lastname">성</label>
                </div>
                <div class="input-field col s6">
                  <input id="firstname" name="firstname" type="text" class="validate">
                  <label for="firstname">이름</label>
                </div>
                <div class="input-field col s12"> 
                  <i class="material-icons prefix">email</i>
                  <input id="email" name="email" type="email" class="validate">
                  <label for="email" data-error="이메일 주소가 올바르지 않습니다">이메일</label>
                </div>
                <div class="input-field col s12">
                  <i class="material-icons prefix">phone</i>
                  <input id="phone" name="phone" type="tel" class="validate">
                  <label for="phone">핸드폰번호</label>
                </div>
                <div class="input-field col s12">
                  <i class="material-icons prefix">lock</i>
                  <input id="password" name="password" type="password" class="validate">
                  <label for="password">비밀번호</label>
                </div>
              </div>
              <div class="center-align">
                <a href="#" class="btn waves-effect waves-light card_btn red accent-2">등록하기 </a>
              </div>
            </form> -->
